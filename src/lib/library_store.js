/**
 * Provide access to library versions across different databases and languages
 */
module.exports.get = function(vendor, library, cb) {
    'use strict';

    // scan across different dbs to find this vendor and library

    // call cb with library object and err parameters
    cb(null, 'not implemented');
};